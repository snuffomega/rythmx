# Rythmx Plugins

Internal plugin contract documentation has moved to:

- `docs/dev/plugin-contract.md`

This `plugins/` copy is intentionally minimal for now. When we publish external plugin author guidance, we can mirror that dev contract back into this directory.
